<%= form_with(model: connect, local: true) do |form| %>
    <% if connect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(connect.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% connect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
      
<!--
  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user %>
  </div>
-->
 <%= form.hidden_field :user, :value=> current_user.email %>



  <div class="field">
    <%= form.label :collection_id %>
    <%= form.number_field :collection_id %>
  </div>

<!--
  <div class="field">
    <%= form.label :listing_id %>
    <%= form.number_field :listing_id %>
  </div>
-->
<%= form.hidden_field :listing_id, :value=> @listing.id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
