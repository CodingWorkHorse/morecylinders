  <% if current_user && current_user.admin?%>

<div style="text-align: center; min-height: 65vh;">
<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table style="margin-left: auto; margin-right: auto;">
  <thead>
    <tr>
      <th>Email</th>
      <th>Role</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>

        <% if current_user && current_user == user %>
        <td style="color: green;"><%= user.email %></td>
        <% else %>
        <td><%= user.email %></td>
        <% end %>
        <td style="color: red;"><%= user.role %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure that you want to remove this user?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>

<% else %>
<div class="error_access_denied">
  <a href="<%= home_path %>">
<img src="http://baumcube.weebly.com/uploads/6/5/8/4/65844733/something-s-not-quite-right-icon_orig.png" alt="404 Page Not Found" width="943px" height="400px" style="text-align:center;"></a>

<h1>Something Doesn't Quite Line Up</h1>
</div>
<div class="error_button_home">
<a href="<%= home_path %>" >Home</a>
</div>
</div>
<% end %>
